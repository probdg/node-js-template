// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema
// Auto-generated from prisma/models/*.prisma - DO NOT EDIT DIRECTLY

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}


// From log.prisma
// Log model
model Log {
  id         String   @id @default(uuid())
  level      String
  message    String   @db.Text
  meta       Json?
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@index([level])
  @@index([created_at])
  @@map("logs")
}


// From post.prisma
model Post {
  id                    Int       @id @default(autoincrement()) @map("ID")
  post_title            String
  post_content          String    @db.Text
  post_excerpt          String?   @db.Text
  post_status           String    @default("draft")
  post_type             String    @default("post")
  post_author           String
  post_name             String
  post_password         String?   @default("")
  comment_status        String    @default("open")
  ping_status           String    @default("open")
  to_ping               String?   @db.Text
  pinged                String?   @db.Text
  post_content_filtered String?   @db.Text
  post_mime_type        String?
  guid                  String?
  post_parent           String?   @default("")
  menu_order            Int       @default(0)
  comment_count         Int       @default(0)
  post_date             DateTime? @default(now())
  post_date_gmt         DateTime?
  post_modified         DateTime  @updatedAt
  post_modified_gmt     DateTime?
  created_at            DateTime  @default(now())
  uuid                  String    @default(uuid()) @map("post_uuid")


  @@index([post_author])
  @@index([post_status])
  @@index([post_type])
  @@index([post_date])
  @@map("wpny_posts")
}


// From user.prisma
// User model
model User {
  id            String   @id @default(uuid())
  email         String   @unique
  username      String   @unique
  password_hash String   @default("")
  role          String   @default("user")
  is_active     Boolean  @default(true)
  created_at    DateTime @default(now())
  updated_at    DateTime @updatedAt

  @@map("users")
}


// From vault.prisma
// Vault model for encrypted credentials
model VaultEntry {
  id              String   @id @default(uuid())
  key             String   @unique
  encrypted_value String
  iv              String
  created_at      DateTime @default(now())
  updated_at      DateTime @updatedAt

  @@map("vault")
}

